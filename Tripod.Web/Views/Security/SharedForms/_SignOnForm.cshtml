@model SignOnFormModel

@{
    var columnWidth = Model.EmailProvidersOnly ? "col-sm-6" : "col-sm-4";
}

<form action="@Url.Action(MVC.SignOn.Post())" method="POST" class="providers row">

    @Html.AntiForgeryToken()

    <input type="hidden" name="returnUrl" value="@ViewBag.ReturnUrl" />

    @* GOOGLE *@
    <div class="@columnWidth">
        @{ Html.RenderPartial(MVC.Security.Views.SharedViews._SignOnGoogleButton); }
    </div>

    @* FACEBOOK *@
    <div class="@columnWidth">
        @{ Html.RenderPartial(MVC.Security.Views.SharedViews._SignOnFacebookButton); }
    </div>

    @* TWITTER *@
    @if (!Model.EmailProvidersOnly)
    {
        <div class="col-sm-4">
            @{ Html.RenderPartial(MVC.Security.Views.SharedViews._SignOnTwitterButton); }
        </div>
    }
</form>

