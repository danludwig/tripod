@using Microsoft.AspNet.Identity
@model ICollection<UserLoginInfo>

@if (Model.Count > 0)
{
    <h4>Registered Logins</h4>
    Html.RenderPartial(MVC.RemoteMemberships.Views._Table, Model);
}
