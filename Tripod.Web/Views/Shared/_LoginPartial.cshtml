@using Microsoft.AspNet.Identity

@if (Request.IsAuthenticated)
{
    <form id="logoutForm" class="navbar-right" method="POST" action="@Url.Action(MVC.Account.LogOff())">
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right">
            <li>
                <a href="@Url.Action(MVC.Account.Manage())" title="Manage">Hello @User.Identity.GetUserName()!</a>
            </li>
            <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
        </ul>
    </form>
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>
            <a id="registerLink" href="@Url.Action(MVC.Account.Register())">Register</a>
        </li>
        <li>
            <a id="loginLink" href="@Url.Action(MVC.Authentication.Login())">Log in</a>
        </li>
    </ul>
}
